/**
 * PluginLoader will allow dynamic import of external modules (plugins).
 * Example: kaimose-plugin-aws, kaimose-plugin-http
 */

export class PluginLoader {
	static loadPlugins() {
		console.log("Loading external plugins...");
	}
}